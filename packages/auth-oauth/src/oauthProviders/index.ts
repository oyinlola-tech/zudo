/**
 * Provider presets and profile normalisation.
 *
 * @module oauthProviders
 */

export {
  type OAuthProviderPreset,
  PROVIDER_PRESETS,
  normalizeUserInfo,
} from "./oauthProvider.presets.js";
