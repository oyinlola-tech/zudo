{"size":5,"pass":4,"questions":[{"id":"client-server","type":"choice","q":"Which sentence describes a <strong>client</strong> and a <strong>server</strong>?","explain":"<p>The client asks and the server answers. A browser, a phone app and <code>curl</code> are all clients. The backend you build is a server.</p>","options":["The client stores the data; the server shows it on the screen","The client sends a request; the server waits for requests and sends back a response","The client is always a browser; the server is always a phone","They are two names for the same program"],"answer":1},{"id":"requests-count","type":"choice","q":"You open a web page that shows ten images, for the first time, and each image is a separate file. How many requests does the browser make to show it?","explain":"<p>One request gets one response. The browser asks once for the page, then once for each image: 1 + 10 = 11. On a later visit it may reuse images it saved, and send fewer.</p>","options":["1","10","11","20"],"answer":2},{"id":"ip-address","type":"choice","q":"What is an <strong>IP address</strong>?","explain":"<p>Every computer on the internet has an IP address, such as <code>104.20.23.154</code>. The name <code>example.com</code> is a domain name, which DNS turns into an IP address.</p>","options":["The name of a website, like <code>example.com</code>","A number that says where a computer is on the internet, like a postal address","The password of your Wi-Fi network","The path of a page, like <code>/tasks</code>"],"answer":1},{"id":"ipv4-valid","type":"choice","q":"Which of these is a valid <strong>IPv4</strong> address?","explain":"<p>IPv4 is four numbers from 0 to 255. <code>300</code> is too big, and three numbers are too few. The form with colons is IPv6.</p>","options":["<code>104.20.23.154</code>","<code>104.20.300.154</code>","<code>2606:4700:10::6814:179a</code>","<code>104.20.23</code>"],"answer":0},{"id":"why-ipv6","type":"choice","q":"Why does IPv6 exist?","explain":"<p>IPv4 has a limited number of addresses and they ran out. IPv6 is a longer form with far more of them.</p>","options":["IPv4 is not encrypted","The world ran out of IPv4 addresses, and IPv6 has many more","IPv6 is only for phones","IPv6 replaces domain names"],"answer":1},{"id":"dns","type":"choice","q":"What does <strong>DNS</strong> do?","explain":"<p>DNS, the Domain Name System, looks up a name like <code>example.com</code> and returns its IP addresses. Encryption is HTTPS's job, and the port picks the program.</p>","options":["Encrypts the connection between browser and server","Turns a domain name into an IP address, like a phone book","Picks which program on a computer gets the request","Stores the data of your application"],"answer":1},{"id":"several-addresses","type":"choice","q":"<code>nslookup example.com</code> shows several addresses for one name. Why do big sites have several?","explain":"<p>If one machine fails, clients can use another address. Your answers can also differ from someone else's: the numbers change over time and between countries.</p>","options":["Each address shows a different website","DNS made a mistake","So that one broken machine does not take the site down","One address is for images, the other for text"],"answer":2},{"id":"localhost","type":"choice","q":"You start a server on your own computer while developing. Which address reaches it?","explain":"<p><code>localhost</code> always means \"this computer\", and its IPv4 address is <code>127.0.0.1</code>. It is how you reach servers running on your own machine.</p>","options":["<code>example.com</code>","<code>localhost</code>, which is <code>127.0.0.1</code>","<code>104.20.23.154</code>","<code>0.0.0.0.0</code>"],"answer":1},{"id":"port-purpose","type":"choice","q":"One computer runs a web server and a database. How does a request reach the right program?","explain":"<p>The IP address finds the computer. The <strong>port</strong>, a number from 1 to 65535, finds the program on it.</p>","options":["By its domain name","By its port number","By its status code","By its HTTP method"],"answer":1},{"id":"port-https","type":"choice","q":"Which port does <code>https://shop.example.com/cart</code> use?","explain":"<p>The URL names no port, so the default for <code>https</code> is used: 443. The default for plain <code>http</code> is 80.</p>","options":["80","443","3000","5432"],"answer":1},{"id":"port-postgres","type":"choice","q":"Which port is the standard one for PostgreSQL, the database used later in the course?","explain":"<p>PostgreSQL listens on 5432. 80 and 443 are for the web, and 3000 or 8080 are common choices for your own server while you develop.</p>","options":["80","443","5432","8080"],"answer":2},{"id":"url-query","type":"choice","q":"In <code>https://api.example.com/tasks?done=false&amp;limit=10</code>, which part is the <strong>query</strong>?","explain":"<p>The query comes after <code>?</code> and holds options as <code>name=value</code> pairs joined by <code>&amp;</code>. <code>/tasks</code> is the path and <code>api.example.com</code> is the hostname.</p>","options":["<code>https</code>","<code>api.example.com</code>","<code>/tasks</code>","<code>?done=false&amp;limit=10</code>"],"answer":3},{"id":"http-method","type":"choice","q":"What does the HTTP <strong>method</strong> <code>GET</code> say?","explain":"<p><code>GET</code> asks for something. <code>POST</code> means \"here is something new\". The method, the path and the headers together describe what the client wants.</p>","options":["\"Here is something new, save it\"","\"Give me\" this resource","\"Delete everything\"","\"Encrypt this request\""],"answer":1},{"id":"response-parts","type":"choice","q":"Which three parts does an HTTP response have?","explain":"<p>A response starts with a status code (how it went), then headers (extra information), then the body: the page, image or data. Method and path belong to the request.</p>","options":["A method, a path and a query","A status code, headers and a body","An IP address, a port and a domain","A protocol, a hostname and a port"],"answer":1},{"id":"status-codes","type":"choice","q":"What do the status codes <code>200</code> and <code>404</code> mean?","explain":"<p><code>200</code> means success. <code>404</code> means the server does not know the path you asked for.</p>","options":["200: not found; 404: OK","200: OK, here it is; 404: not found","200: encrypted; 404: not encrypted","200: a GET request; 404: a POST request"],"answer":1},{"id":"curl-i","type":"choice","q":"What does <code>curl -I https://example.com</code> print?","explain":"<p>With <code>-I</code>, curl asks only for the status line and headers. Without it, curl prints the body, the HTML of the page.</p>","options":["Only the HTML of the page","Only the status and the headers","The IP address of the server","A picture of the page"],"answer":1},{"id":"curl-windows","type":"choice","q":"You are in Windows PowerShell and want the real curl. What do you type?","explain":"<p>In Windows PowerShell, plain <code>curl</code> is a different command with different options. <code>curl.exe</code> is the real curl, which comes with Windows 10 and 11.</p>","options":["<code>curl</code>","<code>curl.exe</code>","<code>Get-Curl</code>","<code>wsl curl</code> is the only way"],"answer":1},{"id":"content-type","type":"choice","q":"A response has the header <code>content-type: application/json</code>. What is in its body?","explain":"<p>The <code>content-type</code> header says what the body is. <code>text/html</code> is a web page; <code>application/json</code> is data, typical for an API.</p>","options":["A web page for people to read","Data in JSON format, made for programs","An image","Nothing, the body is empty"],"answer":1},{"id":"https-adds","type":"choice","q":"What does HTTPS add to HTTP?","explain":"<p>HTTPS sends HTTP through an encrypted TLS connection, so nobody on the way can read or change it. The certificate proves the server's identity: that is the padlock.</p>","options":["Faster pages, because data is compressed","An encrypted connection, and a certificate that proves the server really is who it says","A second port for images","A way to use domain names instead of IP addresses"],"answer":1},{"id":"https-password","type":"choice","q":"Your login form will be used by real people. Which address should it send the password to?","explain":"<p>Plain HTTP travels as readable text, so anyone on the same network or on the way can read it. Every public site and API must use HTTPS. Plain HTTP is only acceptable on <code>localhost</code>.</p>","options":["<code>http://api.myapp.com/login</code>, because it is faster","<code>http://api.myapp.com/login</code>, if the Wi-Fi has a password","<code>https://api.myapp.com/login</code>","Either one, as long as the password is long"],"answer":2},{"id":"http-localhost-ok","type":"choice","q":"When is plain <code>http://</code> acceptable?","explain":"<p>On <code>localhost</code>, the request never travels over a network, so nobody can read it. Everything that real users reach must use HTTPS.</p>","options":["For any page that has no images","For APIs, because programs are not people","On <code>localhost</code>, where the data never leaves your computer","For login forms on small websites"],"answer":2},{"id":"network-tab","type":"choice","q":"Where in the browser can you see every request a page makes, with its method, status code and headers?","explain":"<p>Open the devtools with <kbd>F12</kbd>, choose <strong>Network</strong> and reload the page. Each row is one request; click it to see the details.</p>","options":["The address bar","The Console tab of the devtools","The Network tab of the devtools","The browser's history"],"answer":2},{"id":"api-def","type":"choice","q":"What is a web <strong>API</strong>?","explain":"<p>A web page is made for people; an API is made for programs. It usually answers with JSON, a text format for data that looks like JavaScript objects.</p>","options":["A web page with nice colours","A set of URLs that programs call to read or change data, usually answering in JSON","The program that turns names into IP addresses","A database that runs in the browser"],"answer":1},{"id":"listen-local","type":"choice","q":"The tiny API calls <code>server.listen(0, \"127.0.0.1\")</code>. What does that mean?","explain":"<p>Port <code>0</code> means \"any free port\"; the operating system picks one. <code>127.0.0.1</code> is <code>localhost</code>, so only your own computer can reach the server.</p>","options":["Listen on port 0 only, reachable from the whole internet","Use any free port, and accept connections only from this computer","Stop the server after 0 seconds","Listen on port 127"],"answer":1},{"id":"why-database","type":"choice","q":"The tiny API keeps its tasks in a list inside the program. What is the problem with that for a real app?","explain":"<p>Real data must survive restarts and be shared by many copies of the server. That is what a database is for: a separate program built to store data safely and find it fast.</p>","options":["Lists cannot hold more than two tasks","The tasks are gone when the program stops, and other copies of the server cannot share them","Lists make the server use HTTP instead of HTTPS","The browser cannot read lists"],"answer":1},{"id":"browser-database","type":"choice","q":"In a well-built app, who does the browser talk to?","explain":"<p>The browser never talks to the database directly. The backend stands between them, checks each request and who is asking, and only then reads or changes data.</p>","options":["Directly to the database, because it is faster","To the database and the backend, equally","Only to the backend, which talks to the database and decides what each person may read or change","Only to DNS"],"answer":2},{"id":"journey-order","type":"choice","q":"Someone opens a task app. What happens <strong>first</strong>?","explain":"<p>The browser first needs an address: DNS turns the domain into an IP address. Then it connects on port 443, sets up HTTPS and only then sends the HTTP request.</p>","options":["The backend asks the database for the tasks","The browser sends <code>GET /tasks</code>","The browser asks DNS for the IP address of the app's domain","The server sends a <code>200</code> response"],"answer":2},{"id":"out-url-parts","type":"output","q":"What does this print?","explain":"<p>The hostname is the name only, without the port. The URL names port 8443. The path stops before the <code>?</code>; the rest is the query.</p>","code":"const url = new URL(\"https://api.example.com:8443/tasks?done=false\");\nconsole.log(url.hostname);\nconsole.log(url.port);\nconsole.log(url.pathname);","file":"url.js","options":["api.example.com\n8443\n/tasks","api.example.com:8443\n8443\n/tasks","api.example.com\n443\n/tasks","api.example.com\n8443\n/tasks?done=false"],"answer":0},{"id":"out-protocol","type":"output","q":"What does this print?","explain":"<p><code>url.protocol</code> includes the colon at the end: <code>https:</code>.</p>","code":"const url = new URL(\"https://example.com/\");\nconsole.log(url.protocol);","file":"protocol.js","options":["https:","https","https://","443"],"answer":0},{"id":"out-default-port","type":"output","q":"What does this print?","explain":"<p>The URL names no port, so <code>port</code> is empty. The client then uses the protocol's default, 443 for <code>https</code>. <code>JSON.stringify</code> shows the empty text as <code>\"\"</code>.</p>","code":"const plain = new URL(\"https://example.com/\");\nconsole.log(JSON.stringify(plain.port));","file":"default-port.js","options":["\"\"","\"443\"","443","\"80\""],"answer":0},{"id":"out-search","type":"output","q":"What does this print?","explain":"<p><code>search</code> is the whole query, starting with <code>?</code>. <code>searchParams.get(\"limit\")</code> gives just the value of one option.</p>","code":"const url = new URL(\"https://api.example.com/tasks?done=false&limit=10\");\nconsole.log(url.search);\nconsole.log(url.searchParams.get(\"limit\"));","file":"search.js","options":["?done=false&amp;limit=10\n10","done=false&amp;limit=10\n10","?done=false&amp;limit=10\nlimit=10","/tasks?done=false&amp;limit=10\n10"],"answer":0},{"id":"out-missing-param","type":"output","q":"What does this print?","explain":"<p><code>get(\"q\")</code> finds <code>milk</code>. The query has no <code>page</code> option, and <code>get</code> returns <code>null</code> for a missing name.</p>","code":"const url = new URL(\"http://localhost:3000/search?q=milk\");\nconsole.log(url.searchParams.get(\"q\"), url.searchParams.get(\"page\"));","file":"missing.js","options":["milk null","milk undefined","q=milk null","milk"],"answer":0},{"id":"out-localhost","type":"output","q":"What does this print?","explain":"<p>This URL names a port, 3000, so <code>port</code> is not empty. <code>new URL</code> does not do a DNS lookup, so the hostname stays <code>localhost</code>.</p>","code":"const url = new URL(\"http://localhost:3000/search?q=milk\");\nconsole.log(url.hostname, url.port, url.pathname);","file":"local.js","options":["localhost 3000 /search","localhost:3000 3000 /search","127.0.0.1 3000 /search","localhost 80 /search"],"answer":0},{"id":"out-host-vs-hostname","type":"output","q":"What does this print?","explain":"<p><code>host</code> includes the port when the URL names one; <code>hostname</code> is only the name. DNS looks up the hostname.</p>","code":"const url = new URL(\"http://localhost:8080/tasks\");\nconsole.log(url.host);\nconsole.log(url.hostname);","file":"host.js","options":["localhost:8080\nlocalhost","localhost\nlocalhost:8080","localhost\nlocalhost","localhost:8080\nlocalhost:8080"],"answer":0},{"id":"out-build-url","type":"output","q":"What does this print?","explain":"<p><code>searchParams.get(name)</code> gives the value of one option from the query: <code>true</code> for <code>done</code> and <code>5</code> for <code>limit</code>. The name and the <code>=</code> are not part of the value.</p>","code":"const url = new URL(\"http://localhost:3000/tasks?done=true&limit=5\");\nconsole.log(url.searchParams.get(\"done\"), url.searchParams.get(\"limit\"));","file":"done.js","options":["true 5","done=true limit=5","?done=true&amp;limit=5","true null"],"answer":0},{"id":"out-path-search","type":"output","q":"What does this print?","explain":"<p>The path stops before the <code>?</code>. <code>search</code> is the whole query, and it starts with the <code>?</code>.</p>","code":"const url = new URL(\"https://shop.example.com/products?page=2\");\nconsole.log(url.pathname);\nconsole.log(url.search);","file":"path-search.js","options":["/products\n?page=2","/products?page=2\n?page=2","shop.example.com/products\npage=2","/products\npage=2"],"answer":0},{"id":"out-http-port","type":"output","q":"What does this print?","explain":"<p><code>protocol</code> ends with a colon. This URL names its port, 8080, so <code>port</code> is not empty and the default for <code>http</code> (80) is not used.</p>","code":"const url = new URL(\"http://shop.example.com:8080/cart\");\nconsole.log(url.protocol, url.port);","file":"http-port.js","options":["http: 8080","http 8080","http: 80","https: 8080"],"answer":0},{"id":"out-path-only","type":"output","q":"What does this print?","explain":"<p>The hostname is only the name, without the protocol. The path is everything from the first <code>/</code> after the name up to the <code>?</code>, so it includes <code>42</code> but not the query.</p>","code":"const url = new URL(\"https://api.example.com/tasks/42?fields=title\");\nconsole.log(url.hostname, url.pathname);","file":"path-only.js","options":["api.example.com /tasks/42","api.example.com /tasks/42?fields=title","https://api.example.com /tasks/42","api.example.com /tasks"],"answer":0},{"id":"out-server","type":"output","q":"Run this with Node.js. What does it print?","explain":"<p>The server checks the method and path. <code>GET /tasks</code> matches and gets <code>200</code>. Any other path falls through to the <code>404</code> answer. One request, one response each.</p>","code":"import { createServer } from \"node:http\";\n\nconst server = createServer((request, response) => {\n  if (request.method === \"GET\" && request.url === \"/tasks\") {\n    response.writeHead(200, { \"content-type\": \"application/json\" });\n    response.end(JSON.stringify([{ id: 1, title: \"Buy milk\" }]));\n    return;\n  }\n  response.writeHead(404, { \"content-type\": \"application/json\" });\n  response.end(JSON.stringify({ error: \"Not found\" }));\n});\n\nawait new Promise((resolve) => server.listen(0, \"127.0.0.1\", resolve));\nconst base = `http://127.0.0.1:${server.address().port}`;\n\nfor (const path of [\"/tasks\", \"/nothing-here\"]) {\n  const response = await fetch(base + path);\n  console.log(\"GET\", path, \"->\", response.status);\n}\n\nserver.close();","file":"tiny-api.js","options":["GET /tasks -&gt; 200\nGET /nothing-here -&gt; 404","GET /tasks -&gt; 200\nGET /nothing-here -&gt; 200","GET /tasks -&gt; 404\nGET /nothing-here -&gt; 404","GET /tasks -&gt; 200"],"answer":0},{"id":"out-server-body","type":"output","q":"Run this with Node.js. What does it print?","explain":"<p>The server set status 200 and the <code>content-type</code> header. <code>response.json()</code> turns the JSON text back into an object, which Node prints as <code>{ status: 'ok' }</code>.</p>","code":"import { createServer } from \"node:http\";\n\nconst server = createServer((request, response) => {\n  response.writeHead(200, { \"content-type\": \"application/json\" });\n  response.end(JSON.stringify({ status: \"ok\" }));\n});\n\nawait new Promise((resolve) => server.listen(0, \"127.0.0.1\", resolve));\nconst response = await fetch(`http://127.0.0.1:${server.address().port}/health`);\nconsole.log(response.status, response.headers.get(\"content-type\"));\nconsole.log(await response.json());\nserver.close();","file":"health.js","options":["200 application/json\n{ status: 'ok' }","200 text/html\n{ status: 'ok' }","200 application/json\n{\"status\":\"ok\"}","404 application/json\n{ status: 'ok' }"],"answer":0},{"id":"code-url-parts","type":"code","q":"<p>Use <code>new URL(...)</code> to print the hostname, the port, the path and the value of <code>q</code> for <code>http://localhost:3000/search?q=milk</code>, one per line.</p>","explain":"<p>Use <code>url.hostname</code>, <code>url.port</code>, <code>url.pathname</code> and <code>url.searchParams.get(\"q\")</code>. The port is not empty because the URL names it.</p>","file":"parts.js","starter":"const url = new URL(\"http://localhost:3000/search?q=milk\");\n// print hostname, port, path and the value of q","expected":"localhost\n3000\n/search\nmilk","solution":"const url = new URL(\"http://localhost:3000/search?q=milk\");\nconsole.log(url.hostname);\nconsole.log(url.port);\nconsole.log(url.pathname);\nconsole.log(url.searchParams.get(\"q\"));"},{"id":"code-real-port","type":"code","q":"<p>Use <code>new URL(...)</code> to print the protocol and then the path of <code>https://shop.example.com/cart</code>, one per line. The output must be <code>https:</code> and then <code>/cart</code>.</p>","explain":"<p><code>url.protocol</code> includes the colon at the end, and <code>url.pathname</code> is the path. This URL names no port, so the client would use 443, the default for <code>https</code>.</p>","file":"protocol.js","starter":"const url = new URL(\"https://shop.example.com/cart\");\n// print the protocol, then the path","expected":"https:\n/cart","solution":"const url = new URL(\"https://shop.example.com/cart\");\nconsole.log(url.protocol);\nconsole.log(url.pathname);"},{"id":"code-build-url","type":"code","q":"<p>Print the whole query of <code>https://shop.example.com/search?q=rice&amp;page=2</code>, starting with the <code>?</code>. The output must be <code>?q=rice&amp;page=2</code>.</p>","explain":"<p><code>url.search</code> is the query, from the <code>?</code> to the end. Its options are <code>name=value</code> pairs joined by <code>&amp;</code>.</p>","file":"query.js","starter":"const url = new URL(\"https://shop.example.com/search?q=rice&page=2\");\n// print the whole query","expected":"?q=rice&page=2","solution":"const url = new URL(\"https://shop.example.com/search?q=rice&page=2\");\nconsole.log(url.search);"},{"id":"code-is-secure","type":"code","q":"<p>Print the value of the <code>page</code> option from <code>https://shop.example.com/search?q=rice&amp;page=2</code>. The output must be <code>2</code>.</p>","explain":"<p><code>url.searchParams.get(\"page\")</code> finds one option in the query and gives its value, without the name or the <code>=</code>.</p>","file":"page.js","starter":"const url = new URL(\"https://shop.example.com/search?q=rice&page=2\");\n// print the value of page","expected":"2","solution":"const url = new URL(\"https://shop.example.com/search?q=rice&page=2\");\nconsole.log(url.searchParams.get(\"page\"));"},{"id":"code-query","type":"code","q":"<p>Print the values of <code>done</code> and <code>limit</code> from the query of <code>https://api.example.com/tasks?done=false&amp;limit=10</code>, one per line, like <code>done: false</code> and <code>limit: 10</code>.</p>","explain":"<p><code>url.searchParams.get(name)</code> reads one option from the query. Giving <code>console.log</code> a label and the value as two values puts a space between them.</p>","file":"options.js","starter":"const url = new URL(\"https://api.example.com/tasks?done=false&limit=10\");\n// print done: … and limit: …","expected":"done: false\nlimit: 10","solution":"const url = new URL(\"https://api.example.com/tasks?done=false&limit=10\");\nconsole.log(\"done:\", url.searchParams.get(\"done\"));\nconsole.log(\"limit:\", url.searchParams.get(\"limit\"));"},{"id":"code-domain-parts","type":"code","q":"<p><code>http://example.com/</code> names no port. Print its port with <code>JSON.stringify</code>, so the empty text is visible. The output must be <code>\"\"</code>.</p>","explain":"<p>With no port in the URL, <code>url.port</code> is empty text, and the client uses the default for <code>http</code>, port 80. Printed directly, empty text is just a blank line; <code>JSON.stringify</code> shows its quotes.</p>","file":"empty-port.js","starter":"const url = new URL(\"http://example.com/\");\n// print the port so the empty text shows as \"\"","expected":"\"\"","solution":"const url = new URL(\"http://example.com/\");\nconsole.log(JSON.stringify(url.port));"},{"id":"code-ipv4","type":"code","q":"<p>Print the hostname and the port of <code>http://localhost:8080/health</code> on one line, as two values. The output must be <code>localhost 8080</code>.</p>","explain":"<p><code>localhost</code> means \"this computer\", and 8080 is a common port for a server you run while developing. Two values in one <code>console.log</code> are printed with a space between them.</p>","file":"local-parts.js","starter":"const url = new URL(\"http://localhost:8080/health\");\n// print the hostname and the port on one line","expected":"localhost 8080","solution":"const url = new URL(\"http://localhost:8080/health\");\nconsole.log(url.hostname, url.port);"},{"id":"code-json","type":"code","q":"<p>An API answers in JSON. Turn the <code>health</code> object into JSON text with <code>JSON.stringify</code> and print it. The output must be <code>{\"status\":\"ok\"}</code>.</p>","explain":"<p><code>JSON.stringify</code> turns an object into JSON text, the format APIs send. Printing the object directly would show <code>{ status: 'ok' }</code>, which is not JSON.</p>","file":"to-json.js","starter":"const health = { status: \"ok\" };\n// print health as JSON text","expected":"{\"status\":\"ok\"}","solution":"const health = { status: \"ok\" };\nconsole.log(JSON.stringify(health));"}]}